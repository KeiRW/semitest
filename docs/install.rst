Installation guide
==================

************
Main package
************

The ``scSemiProfiler`` package can be installed via the following commands:

.. code-block:: bash
    :linenos:

    pip install scsemiprofiler


